pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        //برای کتابخونه اندازه گذاری ادکردیم
        maven { url "https://jitpack.io" }
    }
}

rootProject.name = "Note App"
include ':app'
